const base=document.querySelector('meta[name="song-builder-base"]').content;
const token=location.hash.slice(1);history.replaceState(null,'',location.pathname);
const form=document.getElementById('join-form'),status=document.getElementById('join-status');
form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{if(!token)throw Error('Open the full invitation link from the project owner.');const r=await fetch(base+'/api/invitations/redeem',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','X-Room-Join-CSRF':document.querySelector('meta[name="room-join-csrf"]').content},body:JSON.stringify({token,name:document.getElementById('guest-name').value})});const data=await r.json();if(!r.ok)throw Error(data.message||'The invitation could not open.');location.assign(data.url);}catch(e){status.textContent=e.message;status.setAttribute('role','alert');button.disabled=false;}});
